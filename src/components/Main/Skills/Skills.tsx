import SkillIconList from './SkillIconList';

const Skills = () => {
	return (
		<div className="section-component container wrapper">
			<h2 className="font-medium text-xl tracking-widest text-secondTextColor text-center uppercase mb-12">
				Experience with
			</h2>
			<SkillIconList />
		</div>
	);
};

export default Skills;
